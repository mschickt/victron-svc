.git
.gitignore
target
**/*.iml
.idea
.vscode
.DS_Store
README.md
CLAUDE.md
deploy
docker-compose.yml
Dockerfile
.dockerignore
# .env holds device config + secret keys. It is injected at runtime by Docker
# Compose (env_file) and must NOT be baked into the image.
.env
data/
