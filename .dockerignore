.git
.gitignore
target
**/*.iml
.idea
.vscode
.DS_Store
README.md
CLAUDE.md
deploy
docker-compose.yml
Dockerfile
.dockerignore
# application-local.yml IS baked into the image on purpose (real device list +
# keys) and loaded via QUARKUS_PROFILE=local. It stays gitignored, so it is
# never committed — only present in the locally-built image.
data/
